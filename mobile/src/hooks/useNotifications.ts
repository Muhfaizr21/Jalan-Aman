/**
 * useNotifications.ts
 * Clean architecture adapter hook re-exporting useNotifications from NotificationContext.
 * Adheres to SOLID Single Responsibility Principle (SRP) and Dependency Inversion Principle (DIP).
 */

export { useNotifications, NotificationContextValue } from '@/context/NotificationContext';
